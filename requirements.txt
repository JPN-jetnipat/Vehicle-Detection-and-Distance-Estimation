# Pinned environment for the I-JEPA x YOLOv5 project (server: A40, CUDA 12.4, driver 550.x)
#
# Install order matters: install torch/torchvision from the cu124 index FIRST,
# then the rest. See RUNBOOK.md §1 for exact commands.
#
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
#
# Kaggle note: Kaggle notebooks ship their own torch build — do NOT reinstall
# torch there; install only the non-torch deps. P100 has no bf16 (Pascal):
# all our scripts must take --amp-dtype {bf16,fp16,fp32} and default safely.

# --- core (torch installed separately from cu124 index, see above) ---
numpy==1.26.4            # <2.0: classic YOLOv5 not fully numpy-2 safe
opencv-python==4.10.0.84
pillow==10.4.0
pyyaml==6.0.2
tqdm==4.66.5
requests==2.32.3

# --- YOLOv5 (classic repo) runtime deps ---
gitpython==3.1.43
psutil==6.0.0
thop==0.1.1.post2209072238
ultralytics==8.3.55      # imported by classic yolov5 for utils only
pandas==2.2.3
matplotlib==3.9.2
seaborn==0.13.2
scipy==1.13.1

# --- I-JEPA / teacher checkpoint ---
transformers==4.48.0     # IJepaModel support (>=4.47) for facebook/ijepa_vith14_1k
huggingface_hub==0.27.1  # snapshot_download for offline server transfer
timm==1.0.11
einops==0.8.0

# --- eval + logging (offline only; no wandb) ---
pycocotools==2.0.8
tensorboard==2.17.1

ijson==3.3.0            # RAM-safe streaming of the 1.4 GB label JSON

# --- data download ---
kaggle==1.6.17
